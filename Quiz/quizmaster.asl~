// Agent quizmaster in project saluti.mas2j

/* Initial beliefs and rules */

interlocutor(contestant3).
question(A).
question(B).
question(C).
question(D).
question(E).
question(F).
question(G).
question(H).
question(I).
question(J).
noq(1).
contestant1points(0).
contestant2points(0).
contestant3points(0).

/* Initial goals */
!start.

/* Plans */

+!start : true <- 
	.print("Sono il presentatore: che il gioco inizi!");
	.wait(1000);
	!ask_question1.
	
+!ask_question1: noq(K) & K = 1 <- 
	.print("Domanda 1: ");
	//.broadcast(achieve, );
	//.send(Other, tell, saluti(Msg));
	-noq(1);+noq(2);
	!ask_question2.
	
+!ask_question2: noq(K) & K = 2 <-
	.print("Domanda 2: ");
	//.broadcast(achieve, );
	//.send(Other, tell, saluti(Msg));
	-noq(2);+noq(3);
	!ask_question3.

+!ask_question3: noq(K) & K = 3 <-
	.print("Domanda 3: ");
	//.broadcast(achieve, );
	//.send(Other, tell, saluti(Msg));
	!end.

+!end: true <-
	.print("Il gioco è finito, ha vinto l'amicizia!").
	
//+!ask_question: number_of_questions < K & K = 3 & Y = number_of_questions <-
	//-number_of_questions(Y); +number_of_questions(Y).
	
			

